{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Values.rbac.serviceAccount.name | default "easylab" }}
  namespace: {{ include "easylab.namespace" . }}
  labels:
    {{- include "easylab.labels" . | nindent 4 }}
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["services", "pods"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  # The egress-proxy sidecar is configured per workload via a ConfigMap the
  # gateway creates/updates (k8s.CreateProxyConfigMap), and trusts the
  # per-deployment MITM CA stored in a Secret (k8s.EnsureEgressCA).
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Values.rbac.serviceAccount.name | default "easylab" }}
  namespace: {{ include "easylab.namespace" . }}
  labels:
    {{- include "easylab.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ .Values.rbac.serviceAccount.name | default "easylab" }}
subjects:
  - kind: ServiceAccount
    name: {{ .Values.rbac.serviceAccount.name | default "easylab" }}
    namespace: {{ include "easylab.namespace" . }}
{{- end }}
